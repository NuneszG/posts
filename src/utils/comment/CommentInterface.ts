

export interface CommentInteger {
    content: string
    user_username: string
    post_title: string
};

export interface CommentId {
    id: string
};

export interface CommentBody {
    id: string
    content: string
};
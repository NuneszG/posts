
export interface UserInteger {
    id: string
    name: string
    username: string
    email: string
    password: string
    status: boolean
    bio: string
};

export interface UserId {
    id: string
};

export interface Userkeys {
    name: string
};

export interface UserBody {
    name: string
    username: string
    email: string
    password: string
    status: boolean
    bio: string
};
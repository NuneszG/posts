
export interface PostInteger {
    id: string
    title: string
    content: string
    published: boolean
};

export interface PostId {
    id: string
};

export interface PostBody {
    title: string
    content: string
    published: boolean,
    id_category: string
    id: string
};